# API Backend URL
# En développement local : pointe directement vers le serveur Fastify.
VITE_API_URL=http://127.0.0.1:3010

# En production (Vercel mono-projet), NE PAS définir VITE_API_URL :
# le front appelle l'API en relatif (même domaine), sans CORS à gérer.
